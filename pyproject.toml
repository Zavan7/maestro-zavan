[project]
name = "azav-maestro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "celery>=5.6.3",
    "django>=6.1",
    "dotenv>=0.9.9",
    "redis>=8.1.0",
]

[dependency-groups]
dev = [
    "django-stubs>=6.1.1",
]
