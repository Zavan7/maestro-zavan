[project]
name = "azav-maestro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "dotenv>=0.9.9",
]

[dependency-groups]
dev = [
    "django-stubs>=6.1.1",
]
